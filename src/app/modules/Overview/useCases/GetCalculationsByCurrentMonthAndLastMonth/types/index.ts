export type TReturnMonths = {
  currentMonth: number;
  lastMonth: number;
}

export type TGetCardsData = {
  difference: number;
  percent: number;
  currentMonth: number;
  lastMonth: number;
}
